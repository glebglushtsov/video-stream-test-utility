{
  "name": "video-stream-test-utility",
  "version": "1.0.0",
  "description": "Video Stream Test Utility",
  "main": "src/index.js",
  "scripts": {
    "start:server": "nodemon src/server.js 8000",
    "start:ui": "browser-sync --files \"src/**/*.*, css/*.css, index.html\"",
    "start": "npm-run-all --parallel --race start:server start:ui"
  },
  "author": "Gleb Glushtsov",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^3.0.2",
    "nodemon": "^3.1.0",
    "npm-run-all": "^4.1.5"
  }
}
